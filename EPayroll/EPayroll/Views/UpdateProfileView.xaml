<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="EPayroll.Views.UpdateProfileView"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True">

    <ContentPage.Content>
        <StackLayout Orientation="Vertical" BackgroundColor="Aqua">
            <StackLayout Orientation="Horizontal"
                         >
                <Label Text="Thong Tin Tai Khoan" FontSize="30" FontAttributes="Bold" TextColor="Black" HorizontalOptions="CenterAndExpand"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal"
                        >
                <Image Source="{Binding ImageSource}"  HorizontalOptions="CenterAndExpand"></Image>
            </StackLayout>
            <StackLayout Orientation="Vertical" BackgroundColor="Aqua">
                <Label Text="{Binding Name}" FontSize="24" TextColor="Black" FontAttributes="Bold"/>
                <Label Text="{Binding Position}" FontSize="18"/>
            </StackLayout>
            <Grid>
                <!-- Grid.Row="0" and Grid.Column="0" can be omitted -->
                <!-- Left collumn-->
                <Label Grid.Column="0" Grid.Row="0" Text="Tuoi: " Margin="5" TextColor="Black" FontAttributes="Bold" FontSize="Body"/>

                <Label Grid.Column="0" Grid.Row="1" Text="Gioi Tinh: " Margin="5" TextColor="Black"   FontAttributes="Bold" FontSize="Body"/>

                <Label Grid.Column="0" Grid.Row="2" Text="CMND: " Margin="5" TextColor="Black"    FontAttributes="Bold" FontSize="Body"/>

                <Label Grid.Column="0" Grid.Row="3" Text="Bac Luong " Margin="5" TextColor="Black" FontAttributes="Bold"  FontSize="Body"/>

                <Label Grid.Column="0" Grid.Row="4" Text="Loai Luong" Margin="5" TextColor="Black" FontAttributes="Bold"  FontSize="Body"/>

                <!-- Right collumn-->
                <Label Grid.Column="1" Grid.Row="0" x:Name="Tuoi" Text="{Binding Age}"/>

                <Label Grid.Column="1" Grid.Row="1" x:Name="Gioi_Tinh" Text="{Binding Gender}"/>

                <Label Grid.Column="1" Grid.Row="2" x:Name="CMND" Text="{Binding IdentifyNumber}"/>

                <Label Grid.Column="1" Grid.Row="3" x:Name="Level" Text="{Binding Level}"/>
                
                <Label Grid.Column="1" Grid.Row="4" x:Name="Mode" Text="{Binding Mode}"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>